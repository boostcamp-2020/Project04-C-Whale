// import commentAPI from "@/api/comment";

const state = {
  bookmarks: [],
};

const getters = {};

const mutations = {
  SET_BOOKMARKS: (state, bookmarks) => (state.bookmarks = bookmarks),
};

const actions = {};

export default {
  state,
  getters,
  mutations,
  actions,
};
